export const Input = {
    baseStyle: ({colorMode}: {colorMode: string}) => ({
      border: 'none',
      backgroundColor: 'transparent',
      textAlign: 'center',
      outline: 'none',
      maxWidth: 'fit-content',
    }),
  };