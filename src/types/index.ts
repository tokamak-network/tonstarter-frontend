export * from 'types/wallet';

export type StakingTableProps = {
  name: string;
  period: string;
  apy: string;
  total_staked: string;
  earning_per_block: string;
  staked: string;
  earned: string;
};