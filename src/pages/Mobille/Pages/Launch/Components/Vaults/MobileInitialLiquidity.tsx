//MobileInitialLiquidity

import {FC, useState} from 'react';
import {Flex, Box, Text, useColorMode, useTheme} from '@chakra-ui/react';

type InitialLiquidity = {
    vault: any;
    project: any;
  };
  
export const MobileInitialLiquidity: FC <InitialLiquidity> = ({vault, project}) => {
    return (
        <Flex>
            
        </Flex>
    )
}