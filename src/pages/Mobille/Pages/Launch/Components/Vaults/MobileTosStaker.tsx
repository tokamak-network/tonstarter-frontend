import {FC, useState} from 'react';
import {Flex, Box, Text, useColorMode, useTheme} from '@chakra-ui/react';

type TosStaker = {
    vault: any;
    project: any;
  };
  
export const MobileTosStaker: FC <TosStaker> = ({vault, project}) => {
    return (
        <Flex>
            
        </Flex>
    )
}